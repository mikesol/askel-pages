name: Deploy melers to Cloudflare Pages

on:
  push:
    branches: [main]
    paths:
      - "melers-preview/**"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: melers-preview/package-lock.json

      - name: Install dependencies
        working-directory: melers-preview
        run: npm ci

      - name: Build
        working-directory: melers-preview
        run: npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: melers-preview
          command: pages deploy dist --project-name melers --branch main
